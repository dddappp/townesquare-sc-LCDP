// <autogenerated>
//   This file was generated by dddappp code generator.
//   Any changes made to this file manually will be lost next time the file is regenerated.
// </autogenerated>

package xyz.townesquare.sc.domain.user.hibernate;

import java.util.*;
import java.math.BigInteger;
import java.util.Date;
import xyz.townesquare.sc.domain.*;
import org.hibernate.Session;
import org.hibernate.Criteria;
//import org.hibernate.criterion.Order;
import org.hibernate.criterion.Criterion;
import org.hibernate.criterion.Projections;
import org.hibernate.SessionFactory;
import xyz.townesquare.sc.domain.user.*;
import xyz.townesquare.sc.specialization.*;
import xyz.townesquare.sc.specialization.hibernate.*;
import org.springframework.transaction.annotation.Transactional;

public class HibernateUserStateRepository implements UserStateRepository {
    private SessionFactory sessionFactory;

    public SessionFactory getSessionFactory() { return this.sessionFactory; }

    public void setSessionFactory(SessionFactory sessionFactory) { this.sessionFactory = sessionFactory; }

    protected Session getCurrentSession() {
        return this.sessionFactory.getCurrentSession();
    }
    
    private static final Set<String> readOnlyPropertyPascalCaseNames = new HashSet<String>(Arrays.asList("UserWallet", "Username", "ProfileImage", "Bio", "Version", "OffChainVersion", "CreatedBy", "CreatedAt", "UpdatedBy", "UpdatedAt", "Active", "Deleted"));
    
    private ReadOnlyProxyGenerator readOnlyProxyGenerator;
    
    public ReadOnlyProxyGenerator getReadOnlyProxyGenerator() {
        return readOnlyProxyGenerator;
    }

    public void setReadOnlyProxyGenerator(ReadOnlyProxyGenerator readOnlyProxyGenerator) {
        this.readOnlyProxyGenerator = readOnlyProxyGenerator;
    }

    @Transactional(readOnly = true)
    public UserState get(String id, boolean nullAllowed) {
        UserState.SqlUserState state = (UserState.SqlUserState)getCurrentSession().get(AbstractUserState.SimpleUserState.class, id);
        if (!nullAllowed && state == null) {
            state = new AbstractUserState.SimpleUserState();
            state.setUserWallet(id);
        }
        if (getReadOnlyProxyGenerator() != null && state != null) {
            return (UserState) getReadOnlyProxyGenerator().createProxy(state, new Class[]{UserState.SqlUserState.class}, "getStateReadOnly", readOnlyPropertyPascalCaseNames);
        }
        return state;
    }

    public void save(UserState state) {
        UserState s = state;
        if (getReadOnlyProxyGenerator() != null) {
            s = (UserState) getReadOnlyProxyGenerator().getTarget(state);
        }
        if(s.getOffChainVersion() == null) {
            getCurrentSession().save(s);
        } else {
            getCurrentSession().update(s);
        }

        if (s instanceof Saveable)
        {
            Saveable saveable = (Saveable) s;
            saveable.save();
        }
        getCurrentSession().flush();
    }

    public void merge(UserState detached) {
        UserState persistent = getCurrentSession().get(AbstractUserState.SimpleUserState.class, detached.getUserWallet());
        if (persistent != null) {
            merge(persistent, detached);
            getCurrentSession().merge(detached);
        } else {
            getCurrentSession().save(detached);
        }
        getCurrentSession().flush();
    }

    private void merge(UserState persistent, UserState detached) {
        ((UserState.MutableUserState) detached).setOffChainVersion(persistent.getOffChainVersion());
    }

}

