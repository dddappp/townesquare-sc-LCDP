// <autogenerated>
//   This file was generated by dddappp code generator.
//   Any changes made to this file manually will be lost next time the file is regenerated.
// </autogenerated>

package xyz.townesquare.sc.domain.townesquarestate.hibernate;

import java.util.*;
import java.util.Date;
import java.math.BigInteger;
import xyz.townesquare.sc.domain.*;
import org.hibernate.Session;
import org.hibernate.Criteria;
//import org.hibernate.criterion.Order;
import org.hibernate.criterion.Criterion;
import org.hibernate.criterion.Projections;
import org.hibernate.SessionFactory;
import xyz.townesquare.sc.domain.townesquarestate.*;
import xyz.townesquare.sc.specialization.*;
import xyz.townesquare.sc.specialization.hibernate.*;
import org.springframework.transaction.annotation.Transactional;

public class HibernateTownesquareStateStateRepository implements TownesquareStateStateRepository {
    private SessionFactory sessionFactory;

    public SessionFactory getSessionFactory() { return this.sessionFactory; }

    public void setSessionFactory(SessionFactory sessionFactory) { this.sessionFactory = sessionFactory; }

    protected Session getCurrentSession() {
        return this.sessionFactory.getCurrentSession();
    }
    
    private static final Set<String> readOnlyPropertyPascalCaseNames = new HashSet<String>(Arrays.asList("AccountAddress", "IsEmergency", "UserAdmin", "PostAdmin", "OffChainVersion", "CreatedBy", "CreatedAt", "UpdatedBy", "UpdatedAt", "Active", "Deleted", "Version"));
    
    private ReadOnlyProxyGenerator readOnlyProxyGenerator;
    
    public ReadOnlyProxyGenerator getReadOnlyProxyGenerator() {
        return readOnlyProxyGenerator;
    }

    public void setReadOnlyProxyGenerator(ReadOnlyProxyGenerator readOnlyProxyGenerator) {
        this.readOnlyProxyGenerator = readOnlyProxyGenerator;
    }

    @Transactional(readOnly = true)
    public TownesquareStateState get(String id, boolean nullAllowed) {
        TownesquareStateState.SqlTownesquareStateState state = (TownesquareStateState.SqlTownesquareStateState)getCurrentSession().get(AbstractTownesquareStateState.SimpleTownesquareStateState.class, id);
        if (!nullAllowed && state == null) {
            state = new AbstractTownesquareStateState.SimpleTownesquareStateState();
            state.setAccountAddress(id);
        }
        if (getReadOnlyProxyGenerator() != null && state != null) {
            return (TownesquareStateState) getReadOnlyProxyGenerator().createProxy(state, new Class[]{TownesquareStateState.SqlTownesquareStateState.class}, "getStateReadOnly", readOnlyPropertyPascalCaseNames);
        }
        return state;
    }

    public void save(TownesquareStateState state) {
        TownesquareStateState s = state;
        if (getReadOnlyProxyGenerator() != null) {
            s = (TownesquareStateState) getReadOnlyProxyGenerator().getTarget(state);
        }
        if(s.getOffChainVersion() == null) {
            getCurrentSession().save(s);
        } else {
            getCurrentSession().update(s);
        }

        if (s instanceof Saveable)
        {
            Saveable saveable = (Saveable) s;
            saveable.save();
        }
        getCurrentSession().flush();
    }

    public void merge(TownesquareStateState detached) {
        TownesquareStateState persistent = getCurrentSession().get(AbstractTownesquareStateState.SimpleTownesquareStateState.class, detached.getAccountAddress());
        if (persistent != null) {
            merge(persistent, detached);
            getCurrentSession().merge(detached);
        } else {
            getCurrentSession().save(detached);
        }
        getCurrentSession().flush();
    }

    private void merge(TownesquareStateState persistent, TownesquareStateState detached) {
        ((TownesquareStateState.MutableTownesquareStateState) detached).setOffChainVersion(persistent.getOffChainVersion());
    }

}

